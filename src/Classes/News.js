import React, {Component} from 'react';

export class News{
  constructor(){
    if (this.constructor === StockLibrary) {
            throw new TypeError('Abstract class "Widget" cannot be instantiated directly.'); 
    }
  }

  from=(stocksymbols)=>{

    if(typeof stocksymbols ==="string"){

    }
    
     let relatedNews=[];

     //Call the api to get the news from the stocksymbols in para

     //return the relatedNews for the last 2 weeks.

        return relatedNews;
  }

}